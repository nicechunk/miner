# Include only when `nginx -V` confirms a Brotli module and the parent server
# does not already configure it. Always run `nginx -t` before reload.
brotli on;
brotli_static on;
brotli_comp_level 5;
brotli_types application/javascript application/json application/wasm text/css text/javascript text/plain;
